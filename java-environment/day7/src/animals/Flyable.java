package animals;

/*
 * Interfaces are used to provide common functionality across
 * multiple classes, that are not dependent on the heirarchy 
 * of the classes
 */
public interface Flyable {
	/*
	 * By default, interface methods are abstract
	 */
	public void fly();
}
